<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.class_ic.dao.AlarmDAO">
 
<!-- 알림  추가 -->
 <insert id="insertAlarm" parameterType="com.class_ic.vo.All_Alarm_DTO">
     insert into message (messageNo, msContent, msSendTime, email) values ();
 </insert>

 <!-- 알림 삭제 -->
 <delete id="deleteAlarm" parameterType="com.class_ic.vo.All_Alarm_DTO">
 	delete total_count from totalcount where ename=#{user_id} 
 </delete>
 
 <!-- 알림 total count 가져오기 -->
 <select id="totalCount" resultType="Integer" parameterType="String">
 	select count(*) as total_count from alarm where ename=#{user_id} 
 </select>
 
 

select messageNo, msSendTime, eamil from message;

select messageNo, msSendTime, eamil from message where email = #{email} AND msSendTime= #{msSendTime};

insert into messageSend (email, messageNo, deleteStateS) values ();

insert into messageReceive (email, messageNo, readTime, readState, deleteStateR) values ();
 
 
  
</mapper>
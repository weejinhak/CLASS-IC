<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.class_ic.dao.MemberDAO">

<insert id="join" parameterType="com.class_ic.vo.MemberDTO">
	insert into member (email,name,pass,phone,photoSrc) values (#{email}, #{name}, #{pass}, #{phone}, #{photoSrc})
</insert>

<select id="login" resultType="com.class_ic.vo.MemberDTO">
	select * from member where email=#{email} and pwd=#{pwd}
</select>
</mapper>
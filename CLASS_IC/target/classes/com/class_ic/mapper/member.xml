<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.class_ic.app.dao.memberdao">

	<update id="attendanceMember" parameterType="com.class_ic.app.dto.memberdto">
		 UPDATE attendance #{attendState}= NULL WHERE #{attendState}=#{time} and email=#{email}
	</update>

	<select id="login" resultType="Integer">
		select count(*) from member where email=#{id} and pass=#{pw}
	</select>

</mapper>